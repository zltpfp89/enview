/*
// set domain. www.saltware.co.kr -> set domain to saltware.co.kr
var hostname = location.hostname + '';
if( hostname.indexOf('.')!=-1) {
	document.domain = hostname.substring( hostname.indexOf('.')+1);
}
*/
